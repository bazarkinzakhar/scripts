version: "3.9"

services:
  nginx:
    image: nginx:1.25.3
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - web
    restart: unless-stopped

networks:
  web:
